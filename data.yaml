# VisionTextReader — YOLO Dataset Configuration
# Generated by scripts/convert_dataset.py
#
# This file is used by YOLO training frameworks (Ultralytics, etc.)
# to locate images/labels and define the class schema.

# Paths to image directories (relative to this file)
train: datasets/processed/train/images
val: datasets/processed/val/images
test: datasets/processed/test/images

# Number of classes
nc: 1

# Class names (index → label)
names:
  0: text
